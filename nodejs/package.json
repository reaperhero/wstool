{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "## node connection gnatsd ``` const NATS = require('nats') const nc = NATS.connect('nats://testservicenats.myun.tv:4222') nc.publish('foo', 'Hello World!') nc.subscribe('foo', function(msg) {     console.log('Received a message: ' + msg) }) ```",
  "main": "websocket-nats-node.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "websocket-nats": "^0.3.3",
    "ws": "^7.4.6"
  }
}
